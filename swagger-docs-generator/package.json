{
  "name": "swagger-auto-generator",
  "version": "1.0.0",
  "description": "Intelligent swagger documentation generator",
  "scripts": {
    "auto-generate": "node scripts/run-auto-generator.js",
    "serve": "node server/start-server.js",
    "watch": "nodemon --watch ../mock-services --ext js --exec 'npm run auto-generate'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "swagger-ui-express": "^5.0.0",
    "fs-extra": "^11.1.1",
    "glob": "^10.3.3",
    "@babel/parser": "^7.22.0",
    "@babel/traverse": "^7.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
