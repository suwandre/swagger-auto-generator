{
  "name": "swagger-testing-repo",
  "version": "1.0.0",
  "description": "Automated swagger generation with intelligent microservice detection",
  "scripts": {
    "install-all": "npm install && cd swagger-docs-generator && npm install",
    "start-api": "node mock-api-server.js",
    "generate-docs": "cd swagger-docs-generator && npm run auto-generate",
    "serve-docs": "cd swagger-docs-generator && npm run serve",
    "dev": "concurrently \"npm run start-api\" \"npm run serve-docs\"",
    "full-test": "npm run generate-docs && concurrently \"npm run start-api\" \"npm run serve-docs\""
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "joi": "^18.0.1",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
